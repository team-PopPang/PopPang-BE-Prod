# PopPang BE Prod Deployment Runbook

이 문서는 다음 배포 때 같은 절차를 반복할 수 있도록 정리한 루트 런북이다. 민감값은 절대 문서나 채팅에 남기지 않는다.

## Preconditions

- `main`이 `origin/main`에 push되어 있어야 한다.
- 로컬 SSH alias `poppang-server`가 동작해야 한다.
- Docker Desktop/buildx가 동작해야 한다.
- 루트 `.env`에 private repo 접근용 GitHub token이 있어야 한다.

```env
GITHUB_ACCESS_TOKEN=github_pat_...
```

권장 token 설정:

- GitHub Fine-grained personal access token
- Resource owner: `team-PopPang`
- Repository: `PopPang-Private`
- Permission: `Contents: Read-only`

토큰은 채팅, 커밋, 로그에 붙여넣지 않는다. 노출된 토큰은 즉시 revoke한다.

## Safe Private Config Refresh

`make getKey`는 private repo에서 `application-prod.yml`과 Apple `.p8` 파일을 내려받는다. 단, 기존 Makefile은 HTTP 404 응답도 파일로 저장할 수 있으므로 `make getKey` 전에 반드시 `curl -f`로 접근을 검증한다.

```bash
source ./.env
curl -fsSL \
  -H "Authorization: Bearer ${GITHUB_ACCESS_TOKEN}" \
  -o /tmp/poppang-application-prod.yml \
  https://raw.githubusercontent.com/team-PopPang/PopPang-Private/BE/src/main/resources/application-prod.yml

sed -n '1,12p' /tmp/poppang-application-prod.yml | sed -E 's/(: ).+$/\1***MASKED***/'
```

검증이 성공하면 private 파일을 갱신한다.

```bash
make getKey
```

갱신 후 최소 확인:

```bash
sed -n '1,20p' src/main/resources/application-prod.yml | sed -E 's/(: ).+$/\1***MASKED***/'
test -s src/main/resources/auth/AuthKey_382T2TB4RW.p8
```

`application-prod.yml` 내용이 `404`, `Not Found` 등으로 시작하면 배포하지 말고 정상 파일을 복구한다.

## Deploy

배포 버전은 기존 최신 태그보다 높은 버전으로 정한다. 예: `1.2.0`.

로컬 테스트는 JWT secret 환경변수가 필요하다. 실제 운영 secret을 쓰지 말고 테스트용 긴 값을 사용한다.

```bash
JWT_SECRET="$(printf '0%.0s' {1..64})" ./gradlew test
```

테스트가 통과하면 prod 배포를 실행한다.

```bash
make prod-deploy VERSION=1.2.0
```

주의:

- 루트에서 `make`만 실행하면 `getKey`와 `prod-deploy`가 모두 실행된다.
- `makefile`의 `VERSION`이 오래된 값일 수 있으므로, 배포 명령에는 `VERSION=x.y.z`를 명시한다.
- 배포는 Docker image를 `linux/amd64`로 빌드하고 tar로 저장한 뒤 `poppang-server:/home/poppang/opt/deploy/`로 전송한다.
- 서버에서는 `/home/poppang/opt/deploy/deploy-prod.sh`가 `poppang-prod` 컨테이너를 제거하고 새 이미지로 재기동한다.

## Verify

배포 직후 health check:

```bash
curl -i http://poppang.co.kr:4002/actuator/health
```

기대값:

```json
{"status":"UP"}
```

서버 컨테이너 확인:

```bash
ssh poppang-server "docker ps --filter name=poppang-prod --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}'"
```

기대 이미지:

```text
poppang-prod:x.y.z
```

최근 로그 확인:

```bash
ssh poppang-server "docker logs --since 2m poppang-prod | grep -E 'Started|ERROR|Exception|WARN' | tail -80"
```

`Started PoppangBeApplication`이 보이고 반복 재시작 오류가 없어야 한다.

## Tag

배포가 정상 확인된 뒤 release tag를 만든다.

```bash
git tag -a 1.2.0 -m "Release 1.2.0"
git push origin 1.2.0
```

이미 태그가 있으면 새로 만들지 말고 현재 태그가 어느 커밋을 가리키는지 먼저 확인한다.

```bash
git show --no-patch --decorate --oneline 1.2.0
```

## Rollback

health check 실패, 컨테이너 반복 재시작, 주요 endpoint 장애가 확인되면 원인 분석보다 먼저 직전 정상 이미지로 복구한다.

서버에 있는 기존 tar와 image name을 확인한다.

```bash
ssh poppang-server "ls -lh /home/poppang/opt/deploy/*prod*.tar | tail"
ssh poppang-server "docker images --format 'table {{.Repository}}\t{{.Tag}}\t{{.CreatedSince}}\t{{.Size}}' | grep '^poppang-prod'"
```

직전 정상 tar/image로 deploy script를 다시 실행한다.

```bash
ssh poppang-server "bash /home/poppang/opt/deploy/deploy-prod.sh /home/poppang/opt/deploy/poppang-prod-이전버전.tar poppang-prod:이전버전"
```

이후 다시 health check를 확인한다.

```bash
curl -i http://poppang.co.kr:4002/actuator/health
```
